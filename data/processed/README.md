# Processed data

Generated by `scripts/build_processed_dataset.py`:

- `delivery_logistics_processed.parquet` — cleaned segment-level trips (~142k rows)

This artifact is gitignored by default. Rebuild from raw data before training.
